[workspace]
members = [
    "common",    # Wspólne struktury, Protobuf, Serde
    "network", # Logika P2P (libp2p, tokio)
    "cli",     # Aplikacja terminalowa (clap)
    "wasm",    # Kod do przeglądarki (WebAssembly)
    "relay"    # Węzeł pośredniczący ze "skrzynką" do Dockera
]
resolver = "2"
